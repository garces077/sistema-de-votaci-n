# Formen equipos de 3. Van a construir un sistema de votación
# simple, aplicando ramas y versionado.

# Cada integrante crea su propia rama para UNA función:
# - Rama 1: registrar_voto() + validar que no vote 2 veces la
#   misma persona (usa un diccionario)
# - Rama 2: ver_resultados() + mostrar porcentajes, no solo conteos
# - Rama 3: reiniciar_votacion() + guardar historial en un archivo

# Cada uno hace commits con conventional commits, y al terminar,
# marca su rama con un tag personal (ej: v0.1-registro)

# Suban su rama con git push

# Entrega: pantallazo de "git log --oneline --decorate" mostrando
# tus commits y tu tag personal